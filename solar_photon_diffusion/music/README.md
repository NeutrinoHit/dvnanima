# Music drop folder

Place the downloaded SUNO track here as `suno_solar_diffusion.mp3` and run
`bash ../mix_music.sh` from this directory, or `bash mix_music.sh` from the
`solar_photon_diffusion` directory.

The prompt is stored in [`../SUNO_PROMPT.md`](../SUNO_PROMPT.md).
